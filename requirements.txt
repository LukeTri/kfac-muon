# Minimal requirements for KFAC/Muon training scripts.
# Note: install a CUDA 12.8 PyTorch build first when running on GPU hosts.
# Example:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
# Then install the remaining requirements from this file.

numpy>=1.24
torch>=2.2
torchvision>=0.17
Pillow>=9.0
timm>=1.0
datasets>=2.19
